'use client';import{useLocale}from'next-intl';import Link from'@/components/site-link';import{content,pick}from'@/lib/static-content';import type{AppLocale}from'@/i18n/routing';export default function NotFound(){const locale=useLocale()as AppLocale,c=content.notFound,title=pick(c.title,locale).split('\n');return <main className="section not-found"><p className="eyebrow">{pick(c.eye,locale)}</p><h1>{title[0]}<br/>{title[1]}</h1><p>{pick(c.text,locale)}</p><Link className="button" href="/products/">{pick(c.action,locale)} ↗</Link></main>}
